/** 
 * Generated by patsy for patsy-quickstart
 *
 * @version v0.1.2-beta 
 * @date 2013-08-07
 * @link https://github.com/phun-ky/patsy
 */
(function(a) {
    var b = function() {
        var b = Handlebars.compile(a.tmpl.main), c = {
            text: "'Don't be afraid of making mistakes. If you hide your ignorance, no one will hit you and you'll never learn."
        }, d = b(c);
        a("#viewport").prepend(d);
    };
    a(document).ready(function() {
        b();
    });
})(jQuery), function(a) {
    a.extend({
        tmpl: {
            main: '<h1 class="real">{{text}}</h1><h1 class="ghost">{{text}}</h1>',
            tweets: '{{#each tweet}}<blockquote><img class="img-polaroid pull-left" style="margin-right: .9em;" src="{{profile_image_url}}" /><p>{{text}}</p><small>{{from_user}}</small></blockquote>{{/each}}',
            done: "true"
        }
    });
}(jQuery);